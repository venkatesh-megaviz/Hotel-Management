# Optional — only set if bypassing the Vercel /api proxy (direct Render calls).
# Leave unset for normal dev (Vite proxies /api → localhost:4000) and Vercel production.
# VITE_API_URL=https://hotel-management-6rw3.onrender.com
