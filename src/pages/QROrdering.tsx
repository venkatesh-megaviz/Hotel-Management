import ComingSoon from "@/components/ComingSoon";

/**
 * QR Ordering UI is temporarily a Coming Soon placeholder.
 * Backend routes under /api/ordering/qr* remain intact for future use.
 */
export default function QROrdering() {
  return <ComingSoon title="QR Ordering" subtitle="Manage table QR code orders" />;
}
